include <WiFi.h>
#include <ThingerESP32.h>
#define USERNAME "DylanS"
#define DEVICE_ID "dylans"
#define DEVICE CREDENTIAL "123456" //========= WIF]
#define SSID "MECATRONICA 3ABC 5G"
#define SSID_PASSWORD "MEC2025@."  //- PON TU CLAVE REAL
#define LED PIN 2 // LED interno del ESP32
ThingerESP32 thing (USERNAME, DEVICE_ID, DEVICE_CREDENTIAL);
void setup() {
Serial.begin(115200);
pinMode (LED_PIN, OUTPUT);
digita!Write(LED_PIN, LOW); // LED apagado  // Conectar a WiFi
thing.add_wifi(SSID, SSID_PASSWORD);  // Recurso LED (Switch en Thinger.io) 
thing["led"] << digitalPin(LED_PIN)
Serial.println("ESP32 iniciado. Conectando a Thinger.io..."); void loop {
thing.handle();
